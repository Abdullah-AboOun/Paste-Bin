# Production Environment Variables for VPS Deployment
# Copy this to .env.prod on your server

# Docker Image (Optional - uses Docker Hub image instead of building)
# Leave commented to use default, or specify a version:
# DOCKER_IMAGE=abdullahabooun/paste-bin:latest
# DOCKER_IMAGE=abdullahabooun/paste-bin:main-abc1234
# DOCKER_IMAGE=abdullahabooun/paste-bin:v1.0.0

# Database Configuration (runs in Docker container alongside app)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_to_secure_password_here
POSTGRES_DB=app

# Node Environment
NODE_ENV=production

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Application Port
PORT=3000

# VPS DEPLOYMENT NOTES:
# =====================
# 1. Create .env.prod on your VPS with real values:
#    - POSTGRES_PASSWORD: Use a strong password (generate with: openssl rand -base64 32)
#
# 2. Security Best Practices:
#    - Never use default passwords
#    - Keep .env.prod secure (chmod 600 .env.prod)
#    - Never commit .env.prod to git
#    - Use firewall to restrict database port access
#
# 3. Deployment:
#    - Copy this file to your VPS as .env.prod
#    - Update the values with secure credentials
#    - Run: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# 4. Database Backups:
#    - Set up automated backups of the postgres_data_prod volume
#    - Use: docker-compose exec db pg_dump -U postgres app > backup.sql
#
# 5. Initial Setup:
#    - After first deployment, run migrations:
#      docker-compose exec app bun run db:push
